import { Animation, Easing } from 'react-native-animatable';

export const ANIMATABLE_CONFIG = {
  animation: 'fadeIn' as Animation,
  easing: 'ease-in-out' as Easing,
  duration: 200,
  useNativeDriver: true,
};
